spring:
  application:
    name: matriz
  datasource:
    url: jdbc:postgresql://localhost:5436/seg
    username: user
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 9098
